## REMOVED Requirements

### Requirement: Inline 语法桩

**Reason**: `syntax-inline` 规范随官方 `inline` 宏库迁至 `go-macro-contrib` 仓库。

**Migration**: 见 contrib 仓 `openspec/specs/syntax-inline/spec.md`。

### Requirement: InlineExpand 表达式展开

**Reason**: 同上。

**Migration**: 见 contrib 仓 `openspec/specs/syntax-inline/spec.md`。

### Requirement: 可选官方宏库与引入方式

**Reason**: 同上。

**Migration**: 见 contrib 仓 `openspec/specs/syntax-inline/spec.md`。

### Requirement: 与框架边界

**Reason**: 同上。

**Migration**: 见 contrib 仓 `openspec/specs/syntax-inline/spec.md`。

### Requirement: mactest 单测

**Reason**: 同上。

**Migration**: 见 contrib 仓 `openspec/specs/syntax-inline/spec.md`。
