## REMOVED Requirements

### Requirement: contrib 独立子 module

**Reason**: 该能力规范归属 `go-macro-contrib` 仓库，不再由 `go-macro` 主规范集维护。

**Migration**: 见 `github.com/arcane-craft/go-macro-contrib` 仓库 `openspec/specs/macro-contrib/spec.md`。

### Requirement: 官方宏库路径

**Reason**: 同上。

**Migration**: 见 contrib 仓 `openspec/specs/macro-contrib/spec.md`。

### Requirement: 官方宏库与 cmd/macro expand 集成

**Reason**: 同上。

**Migration**: 见 contrib 仓 `openspec/specs/macro-contrib/spec.md`。

### Requirement: contrib 独立测试

**Reason**: 同上。

**Migration**: 见 contrib 仓 `openspec/specs/macro-contrib/spec.md`。

### Requirement: contrib 依赖 go-macro 核心版本

**Reason**: 同上。

**Migration**: 见 contrib 仓 `openspec/specs/macro-contrib/spec.md`。
